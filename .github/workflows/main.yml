name: Upload Website

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
#     - uses: jakejarvis/s3-sync-action@master
#       with:
#         args: --acl public-read --follow-symlinks --delete --exclude '.git*/*'
#       env:
#         AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
#         AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        
    - name: AWS S3 Create Bucket
      # You may pin to the exact commit or the version.
      # uses: aitorres/aws-s3-create-bucket@bf2701c2732789d5924ae02b68976343a31f6db5
      uses: aitorres/aws-s3-create-bucket@v1.2.0
      with:
        # Name of the bucket that will be created (make sure it is unique)
        bucket-name: "vishwas"
        # AWS Access Key ID
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        # AWS Secret Access Key
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        # AWS Region (default: us-east-1)
